# Legacy Streamlit Prototype

This directory preserves the previous Streamlit version of PocketPal.

It remains available as a reference prototype, but the active full-stack
application now lives at the repository root in `backend/`, `mobile/`, and
`supabase/`.
